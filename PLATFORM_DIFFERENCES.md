# Platform differences

## Keyboard modifiers

Linux uses Control for the macOS Command shortcut layer and Alt for Option.
Shortcuts that use both macOS Control and Command map to Super+Control so the
chord stays distinct. The shared Linux command catalog owns this translation;
Ghostty keybindings are not a parallel application-command route.

This register starts empty of approved product differences. Toolkit mechanics
do not count as user-visible differences.

| Surface | macOS behavior | Linux behavior | Reason | User effect | Status |
| --- | --- | --- | --- | --- | --- |
| Shortcut modifier labels | Uses Command/Option glyphs and macOS menu conventions. | GTK accelerators must map the same command model to Linux Control/Alt conventions while displaying platform-correct labels. | Linux keyboards and GTK do not provide the Command modifier. | Muscle-memory differs only where the physical platform requires it; action names and catalog IDs remain identical. | proposed; verify with Sarah during shortcut pass |
| Primary-window controls | Uses macOS traffic lights and their platform-standard placement. | The app-owned GTK client-side titlebar retains native minimize, maximize, and close controls in the desktop-standard end position. | Reproducing macOS traffic lights would discard native Linux window semantics and conventions. | Titlebar ownership, height, hierarchy, and content match; control glyphs and placement remain platform-native. | implemented on X11 and native Wayland; paired-reference verification pending |
| Notifications/settings links | Uses `UNUserNotificationCenter` and macOS System Settings deep links. | Will use the freedesktop notification portal/service and desktop-specific settings route when available. | macOS frameworks are unavailable. | Permission/remediation UI may open a different system panel; policy and wording stay as close as the platform permits. | not started |
| Update distribution | Developer ID, notarized DMG, Sparkle, Homebrew cask. | Native Linux packaging/desktop integration; update mechanism undecided. | Apple signing and Sparkle bundle flow are macOS-specific. | Installation/update mechanics differ; in-app product behavior must not. | not started; decision required in packaging phase |
| Accessibility API | VoiceOver/AppKit accessibility. | AT-SPI via GTK4 accessible roles, names, descriptions, relations, and state. | Platform accessibility stacks differ. | Equivalent keyboard and screen-reader outcomes are required, not API identity. | vertical-slice labels/buttons implemented; full Orca pass pending |
| Footer file/editor opening | Finder and Launch Services discover app bundles and reveal the focused path. | Files/default file handler uses GIO; supported editors are discovered as executable commands on `PATH`. | Linux desktop applications do not have macOS bundle identifiers or Finder. | The same path can be opened, revealed, or copied; available editor names depend on installed Linux command launchers. | implemented; desktop matrix still needs testing |
| Footer font and status glyph outlines | Uses the macOS system monospaced face and SF Symbols. | Uses Noto Sans Mono with DejaVu Sans Mono/generic fallbacks and portable GTK/Unicode status glyphs at the same measured sizes and weights. Provider identities use owned Cairo paths matching the reference shapes, not platform symbols. | Apple's system fonts and SF Symbols are not redistributable Linux dependencies. | Text density, hierarchy, spacing, color, and meaning match; only system-font and remaining status-symbol outlines vary slightly with the installed Linux font stack. | implemented; provider matrices inspected in Latte, Mocha, and HighContrast |
| Hidden-sidebar animation host | AppKit split-host clip view and Core Animation spring animator. | GTK `Overlay` reparents the live sidebar above the terminal and uses motion-controller edge/occupancy state. | AppKit view and animation primitives are unavailable. | Left/right edge activation, no terminal resize, occupancy, 220 ms leave grace, and attention discovery are retained; animation timing may look different until the reduced-motion/theme pass. | behavior implemented; visual matrix pending |
| Terminal display backend on the current development machine | AppKit/Metal. | GTK uses native Wayland/EGL on COSMIC and retains X11/GLX coverage. | Linux uses Ghostty's OpenGL renderer. Installed GTK 4.14 requires the canonical pre-init desktop-GL environment selection to avoid its incompatible GLES path. | No XWayland layer is required; terminal rendering, input, clipboard, and compositor-scale integration passed natively. Omarchy Wayland remount testing preserved a live job and scrollback across eight GTK remounts. Full private Xvfb `:1` and headless Weston `wayland-awesomux-qa` preflight passed terminal integration, post-remount focus transfer, and 100 lifecycle cycles on both displays; X11 exercised the isolated clipboard. If the original GL context is lost, the affected pane shows a close-and-reopen error; final close abandons stale GPU handles while retiring the terminal. | Rendering verified at 100%, 125%, 150%, and 200%; one-shot fault recovery and detached close passed native Wayland integration; the real app Close Pane path preserved original shell/job PIDs through split and resize actions while retiring only the sibling under an isolated native Omarchy run; true lost-context quarantine remains untested. Full isolated X11/Wayland preflight passed exit 0; live Omarchy compositor focus and broader daily-use acceptance remain pending |
| Agent event file observation | Dispatch filesystem source backed by kqueue. | A bounded 150 ms background poll reads only newly appended bytes from the owner-only pane event file. | Linux Foundation does not expose the macOS kqueue-backed file source with equivalent append semantics; polling avoids adding an unpinned watcher dependency. | Provider state may appear up to roughly 150 ms later; protocol, identity, ordering, and privacy behavior are unchanged. | implemented and covered by real-app event ingestion |

No difference is accepted merely because GTK defaults differ visually.
