# Feature parity matrix

Baseline: macOS `fed33ff47c559344fc6db6fa53f16e75fcc4a116`.
Statuses describe working Linux behavior, not documentation completeness.
“Catalog baseline” means the exact entry in
`shared/resources/text-baseline.json`; paraphrases in this matrix are never a
license to change UI copy.

| Feature or surface | macOS source of truth | Exact wording involved | Expected behavior | Linux implementation location | Status | Verification/evidence | Platform difference |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Single-window app shell | `docs/architecture.md`; `Sources/awesoMux/App/PrimaryWindowSupport.swift` | `awesoMux` | One primary window; workspaces replace tab/window proliferation. | `swift-gtk/Sources/AwesoMuxApp/` | partial | Real GTK launch and inspected screenshot; two workspace pages switch inside one window | None intended |
| Launch and first-run empty state | `Sources/awesoMux/Views/ContentView.swift`; catalog baseline | `New Workspace`; catalog empty-state copy | Launch restores or shows the same actionable welcome/empty state. | `swift-gtk/.../Views/EmptyState` | not started | Clean-profile launch screenshot and keyboard walk | None intended |
| Quit | `AppDelegate.swift`; quit-risk policies | `Quit awesoMux?`; catalog dialog copy | Quit gates risky work, flushes approved state within bounded time, preserves daemon sessions. | `swift-gtk/.../App/QuitCoordinator` | not started | Busy/idle quit integration tests | Desktop quit plumbing differs internally |
| Window close | ADR-0002; `AwesoMuxApp.swift` | `Close Window` | With no selected workspace, close the app window without rewriting pane semantics. | `swift-gtk/.../Commands` | not started | Empty-state accelerator/menu test | Linux accelerator label differs |
| Crash recovery | `SessionPersistence.swift`; architecture persistence section | Catalog quarantine/recovery copy | Preserve prior safe state, quarantine bad files, expose truthful recovery choices. | `AwesoMuxCore.SessionStore`; `SessionPersistenceCoordinator` | partial | Corrupt-current/previous, oversized-file, quarantine-permission, and two-launch restore tests pass. A serial 500 ms trailing-edge writer keeps JSON work off GTK, coalesces bursts, flushes the latest clean-run state, and retries failures under focused tests. Recovery UI and forced-kill integration remain pending | XDG state path |
| Sidebar permanent host | ADR-0025; `SidebarSplitController.swift` | `Collapse/Expand Sidebar`; `Hide/Show Sidebar` | Stable sidebar host, persistent width, collapsed rail, hover reveal without resize storms. | `swift-gtk/Sources/AwesoMuxApp`; `AwesoMuxCore.ChromeModels`; `AwesoMuxCore.AppPreferences`; `AwesoMuxCore.CommandCatalog` | partial | Native GTK split implements tested 296/60/250 width policy, 480-point terminal minimum, defensive width/hidden persistence, native commands, and a dedicated 60-point rail. Configured left/right placement mirrors titlebar, divider math, resize ownership, and borders. Hidden mode reparents the sidebar into a GTK overlay for 40-point edge reveal without terminal resize, holds while occupied, and applies 220 ms leave grace. A tested dormant attention policy exposes an edge tab. Real X11 captures verify both left/right hidden attention tabs, physical pointer reveal, and retraction while terminal geometry remains stable. Direct AT-SPI inspection verifies the visible focusable `Show Sidebar` button, needs-input description, and click action | GTK overlay/split primitives replace AppKit split/animation internals |
| Sidebar workspace groups | `SessionGroup.swift`; `SidebarGroupView.swift`; `WorkspaceGroupCreateSheet.swift`; `WorkspaceGroupRenameSheet.swift` | `New Workspace Group…`; group dialog catalog copy | Create, name, collapse, expand, close, color, and order groups. | `AwesoMuxCore.SessionModel`; `AwesoMuxCore.WorkspaceGroupNameDraft`; Swift app sidebar/sheets | partial | Shared mutations now test sanitized unique create/rename, explicit color, stable-ID ordering, populated close with deterministic replacement selection, and workspace moves. The main-window-owned create/rename sheets implement the reference input clamp, sanitized-form feedback, case/diacritic-insensitive duplicate and mixed-script rejection, exact headings/field/actions/hints, guarded Return/Escape, and command gating. Live AT-SPI blocked duplicate `Local`, created and persisted `Research`, renamed and persisted it as `Product`, verified semantics and app survival, and both Latte sheets were inspected. GTK group headers expose truthful create/rename/color/move/close actions, confirmation, dynamic edge enablement, and per-group creation rows hidden during filtering. The first group's real popover was captured through AT-SPI: exact routes and color order are present, Blue is selected, unavailable SSH is absent, Move Up is insensitive at the boundary, and Move Down remains sensitive. A tested sibling overlay replaces the count with a 24-point Close Group action on hover/focus, rests visible for expanded empty groups, and is suppressed during filtering/collapse/drag. Native GTK DnD supports group midpoint reorder plus workspace header drops with nonce validation, filter lockout, no-op rejection, insertion styling, and status announcements. Automatic tint cycling reserves mauve/peach and honors the awesoMux special case. Collapsed groups roll Needs Input/Error/Thinking up to a prioritized visible and spoken signal; collapsed-rail group buttons open a keyboard-accessible live workspace roster with explicit Jump actions. Physical DnD/Orca execution remains pending | GTK menu popover is the non-pointer roster equivalent |
| Workspace ordering | `SessionStore*`; sidebar projections | Catalog reorder/accessibility copy | Reorder within/across groups without losing selection or pane routing. | shared models + GTK row actions and native DnD | partial | Tested identity-based mutation and action availability expose bounded Move Workspace Up/Down plus named previous/next-group and arbitrary-group alternatives. GTK reorders existing rows/rail projections without rebuilding terminal runtimes and refreshes action boundaries. Native sources/targets use midpoint post-removal indices, reject no-ops/external payloads, disable while filtering, and announce tested reference wording. Physical drag execution remains pending because remote XTest events do not reach the app | GTK native DnD mechanics |
| Pinned section | `SidebarPinnedProjection.swift`; shortcut catalog | `Pinned`; `Pin Workspace`; `Unpin Workspace` | User-ordered pinned projection, never duplicates origin row. | `AwesoMuxCore.SidebarLiftedProjection` + GTK synthetic section | partial | Backward-compatible ordered IDs, identity validation, pinned-first deduplication, tested search/top-match order, origin labeling/tint, secondary-click Pin/Unpin, bounded identity-based Move Workspace Up/Down actions, and native midpoint drag reorder with status announcements. Physical drag execution and direct keyboard shortcuts remain pending | None intended |
| Needs Input section | `SidebarAttentionProjection.swift`; ADR-0007 | `Needs Input` | Opt-in arrival-ordered lift for blocking input and unanswered turns without changing group ownership. | `AwesoMuxCore.SidebarLiftedProjection` + GTK synthetic section | partial | Tested state transitions append arrivals without displacing existing rows; one authoritative ordered list drives projection/navigation after Pinned and prevents stale raw pane state from resurrecting acknowledged rows. Passive dwell retains the selected row through a runtime-only sticky until navigation away. Blocking `permissionPrompt`/`userInputRequired` events use real attention reasons; a separate `notification` + `waiting` signal records a runtime-only unanswered turn, honors Pinned precedence, retracts on an answer/end/acknowledgement, and announces exact promotion/return wording without focus theft. Real blocking and unanswered events visibly lifted their target workspaces; live event pairs emitted the distinct exact blocking-input and unanswered-turn AT-SPI promotion copy plus exact return copy at medium priority. Audible Orca transition inspection remains pending | None intended |
| Sidebar search/filter | `SidebarSearch*`; `SidebarView.swift` | Catalog search/filter/empty copy | Fuzzy/filter search uses the same coarse live titles rendered by rows. | `AwesoMuxCore.SidebarFuzzyMatcher`; `AwesoMuxCore.SidebarSearchProjection` + GTK search entry | partial | Styled 30-point expanded field uses exact `Search sessions`; the project-owned ordered-character fuzzy scorer tests word-boundary/contiguous bonuses, bounded gap penalties, later better alignments, diacritics, and query bounds. Visible title/location hits sort by score with a stable source-order tie-breaker and publish every Unicode-safe Pango byte range for bold/underlined regular and lifted rendering. Whole-group and hidden workspace/provider/state/SSH matches preserve authoritative order without invented visible highlights. The deterministic projection retains whitespace/no-result handling; GTK renders exact no-match copy and `Clear search`; Escape clears/restores terminal focus, Up/Down moves a visible result outline, and Return selects through projection order. Real captures cover a fuzzy multi-range title highlight, Pinned title highlight, and no-matches panel; AT-SPI `EditableText` drove each query and the named Clear action returned true and emptied it. A tested zero-group policy drives the reference collapsed dashed 40-point creation action and centered first-launch workspace recovery surface; both were inspected in the real app. Ghostty title/cwd callbacks refresh the authoritative pane snapshot, row/search/accessibility/window/footer surfaces behind stable generation guards. Collapsed search opens a filtered palette | None intended |
| Sidebar accessibility | ADR-0006; `SidebarView.swift` | `Workspaces`; row/group catalog labels | Selected traits, row position, group state, explicit focus order, arrows/Home/End, rotor-equivalent navigation. | Native GTK list/tree-item rows, buttons/search, and `SidebarKeyboardNavigationPolicy` | partial | Rows, lifted projections, rail controls, group disclosures, creation controls, edge attention, and footer controls publish explicit names/descriptions plus selected/expanded state where applicable. Workspace rows construct with list/tree-item roles; groups are level-one tree items. Native position/set-size relations are mirrored as `Position N of M` descriptions because GTK 4.14 does not expose integer relations through AT-SPI `GetRelationSet`; direct bus inspection verified roles, color, disclosure state, singular/plural counts, lifted/group positions, and explicit Shift+F10 action-menu descriptions. Exact expanded/collapsed create and collapsed-search hints are centralized and bus-verified; decorative `+`/`×` children can no longer override their parent action names. The exact Focus Sidebar command unhides and transfers terminal focus to expanded search or the selected/fallback rail row; direct GTK-action QA and real captures verify both native focus rings. Up/Down/Home/End traverse the synthetic hierarchy through a tested policy; empty-search arrows route directly rather than relying on GTK bubbling, and real AT-SPI inspection confirmed first-row focus. Compact targets are at least 24 points. Menu/Shift+F10 opens the same named action surface as secondary click, including non-drag reorder/move routes; its row controller captures those keys ahead of GTK built-ins. Workspace/group moves and status/disclosure transitions announce through GTK; a live unanswered-turn event was captured on the AT-SPI bus with exact text, medium priority, and no focus change. Collapsed Control-1…9 actions share the lifted-first projection used by transient digits. Physical held-modifier QA and full audible Orca navigation remain pending | AT-SPI instead of VoiceOver rotor; GTK integer set relations require description fallbacks |
| Multi-pane workspace peek | `SidebarSessionPeekCard.swift`; `PanePeekItem.swift`; `SidebarSessionTile.swift` | `Jump to pane N`; provider/state/active-pane fragments | Expanded multi-pane rows reveal a delayed, hittable pane-tree-order card; regular and lifted projections share exact pane routing; keyboard/screen-reader users receive equivalent per-pane actions. | `AwesoMuxCore.SidebarPanePeekItem`; GTK workspace rows/context surfaces | partial | Pure projection test covers order, numbering, title sanitization, active identity, local/remote and provider/state wording. Real X11/GLX inspection covers the reference-width card on a lifted Pinned row with two real Ghostty panes; full preflight passes 59 tests and lifecycle/integration gates. Physical pointer click/handoff and Orca action invocation remain pending. | Native GTK popover presentation; no intended behavior difference |
| Workspace selection acknowledgement | ADR-0003 | `Acknowledge Workspace`; `Mark as Read` | 500 ms pane-scoped dwell; fast cycling does not acknowledge; explicit command is immediate. | snapshot acknowledgement IDs + GTK dwell/command | partial | Persistent pane-scoped identity model, pruning on state exit, 500 ms selection generation guard, runtime-only selected-row sticky, blocking-prompt protection, immediate Ctrl-Shift-K workspace sweep, menu action, and multi-pane reducer tests. Full preflight passes 74 Swift tests; injected-clock GTK test and AT-SPI announcement remain pending | None intended |
| New workspace | shortcut catalog; `SessionStore+WorkspaceCommands.swift`; `NewWorkspaceSplitButton.swift`; `SidebarView.swift` | `New Workspace` | Add/select local workspace with one terminal. | `AwesoMuxCore.WorkspaceCreationTarget` + command router + GTK split/menu controls | partial | Native actions create, select, focus, persist, and render one terminal. The expanded primary action resolves the selected workspace's owning group at activation with canonical `awesoMux` fallback; app/menu `New Workspace` targets that canonical default directly, and `New Workspace in Current Directory` combines selected-owner/default routing with the focused cwd. Pure tests reject silent first-stored-group fallback and cover case/diacritic-insensitive default matching. Real AT-SPI activation in a three-group profile appended and selected exactly one workspace in the selected second group, preserving sibling counts and the launch directory. Expanded 30+24 split segments and the collapsed 40-point menu expose exact group creation/targeting routes with a 400 ms duplicate guard. The routed `New Workspace Group` command presents the validated owned naming sheet | Shortcut modifier mapping only |
| New workspace in cwd | shortcut catalog | `New Workspace in Current Directory` | Seed from focused pane's authoritative displayed cwd. | command router | partial | Focused stored cwd seeds a new local workspace and inspected path bar; remote/cwd-missing cases pending | None intended |
| Rename workspace | shortcut catalog; sheets | `Rename Workspace…` | Validate/sanitize and update all name-bearing surfaces coherently. | `SessionSnapshot.renameWorkspace` + GTK modal sheet/command | partial | Sanitized model mutation permits duplicate identity, rejects empty/control-only input, refreshes regular, lifted, rail, search, and persisted representations, and routes Ctrl-Shift-R through awesoMux. The main-window-owned modal sheet now matches exact heading/field/Cancel/Save/empty-hint copy, prevents stacked commands, and has guarded Return/Escape paths. Live AT-SPI verified roles/names, disabled empty Save, successful persisted Save, non-mutating Cancel, command recovery, and crash-free teardown; the final owned Latte surface was inspected. Physical Return/Escape key delivery remains pending | None intended |
| Duplicate workspace | `AwesoMuxApp.swift`; command palette sources | Catalog baseline | Duplicate safe workspace intent without sharing live-only identities. | shared models | not started | Identity/daemon non-alias tests | None intended |
| Move workspace | session reducers/context menus | Catalog baseline | Move within/between groups while preserving focus and ordering. | shared models + sidebar | partial | Context-menu and native pointer routes preserve terminal/pane identity, destination ordering/tint, counts, runtime ownership, and menu routing. Regular/attention rows expose bounded Move Workspace Up/Down and named Move to Previous/Next Group alternatives, with tested availability and refreshed edge enablement. DnD uses stable midpoint indicators and one authoritative post-removal mutation; exact status wording is tested. Physical drag/Orca execution and shortcuts remain pending | None intended |
| Soft-close workspace | ADR-0002; recently-closed reducer | `Close Workspace` | Confirm only when at risk; retain eligible reopen record for 24 h. | `WorkspaceCloseRiskPolicy`; Ghostty shim/runtime; snapshot close records + GTK lifecycle | partial | Row/Ctrl-Shift-W and row close share a pure risk gate driven by real foreground PID/process-tree classification, observed OSC-133 prompt-away state, and fresh agent execution. The staged canonical Ghostty build installs its own shell-integration resources and exposes a locked prompt-observed bit, so startup's ambiguous not-at-prompt default is ignored. Risky close presents one owned non-stacking sheet with exact bidi-safe title/body/hint, safe-default Cancel, named destructive action, and guarded Super-Return/Escape paths; safe close proceeds directly. Soft close removes every projection, tears down its GTK/Ghostty surfaces, preserves a bounded 24-hour recovery snapshot, selects a stable replacement, and fresh reconstruction must succeed before reopen drains the record. Live AT-SPI verified Cancel, persisted close, command recovery, fresh reopen, and app survival; the final sheet was inspected. A real terminal harness proves observed idle prompt safety followed by foreground PID/prompt-away risk. Daemon-backed continuity remains deferred with `amx` | Super replaces Command internally; visible reference hint remains exact |
| Reopen closed workspace | ADR-0015; shortcut catalog | `Reopen Closed Workspace`; `Recently Closed` | Restore most recent or selected eligible record with fresh collision-safe runtime identities. | snapshot recovery model + GTK command | partial | Ctrl-Shift-T is enabled only with a non-expired record; reopen drains only after GTK reconstruction succeeds, restores group order/focus, and handles same-run or restored snapshots. Specific-record chooser and daemon identity continuity remain pending | None intended |
| Permanently clear workspace | shortcut catalog | `Clear Workspace`; catalog confirmation | Always confirm, terminate owned sessions, create no reopen record. | shared mutation + owned GTK destructive sheet/teardown | partial | Row/Ctrl-Alt-Shift-W always shows the exact bidi-safe permanent-close title, risk-sensitive body, keyboard hint, safe-default Cancel, and named destructive action in the shared owned sheet. Live AT-SPI cancellation verified command recovery and no mutation; the final Latte surface was inspected. Confirmed model/UI teardown removes every projection/recovery identity and releases owned GTK/Ghostty surfaces. Remote-owned survival copy remains pending because remote execution is not implemented | Super replaces Command internally; visible reference hint remains exact |
| Terminal pane layout tree | ADR-0026; `TerminalPaneLayout*` | Pane labels from catalog | Closed terminal/document-group taxonomy with safe exhaustive operations. | `shared/product-contract/Workspace` | not started | Ported pure-model tests | None intended |
| Split right | shortcut catalog; `PaneLayoutReducer.splitActivePane`; app command routing | `Split Right`; `Split pane right` | Create a concurrent pane, inherit focused cwd/execution intent where allowed, focus new pane. | shared model + GTK application action/remount | partial | Ctrl-D/app action now mints one local Ghostty surface from the focused cwd, preserves the existing surface, remounts a 0.5 side-by-side split, focuses the new identity, persists it, and refreshes row/peek/footer projections. Real action QA verified two IDs, selected-new identity, inherited cwd, clean close/remount, and the inspected sidebar pane-count state | Remote execution-plan inheritance waits for the real SSH route |
| Split down | shortcut catalog; `PaneLayoutReducer.splitActivePane`; app command routing | `Split Down`; `Split pane down` | Same contract with horizontal divider. | shared model + GTK application action/remount | partial | Ctrl-Shift-D/app action uses the same identity-preserving lifecycle with a top/bottom GTK split. A real right-split/close/down-split/close sequence persisted the expected horizontal then vertical axes and completed without GTK/runtime diagnostics | Remote execution-plan inheritance waits for the real SSH route |
| Split resize/reflow | `PaneLayoutReducer.resizeActiveSplit`; terminal bridge + divider tokens | `Grow Active Pane`; `Shrink Active Pane` | Mouse/keyboard resize, clamped fractions, correct Ghostty pixel/cell resize and reflow. | `PaneLayout.resizingNearestSplit`; exact split-fraction replacement; GTK command/remount and divider-release routes | partial | Control-Alt-Equals/Minus actions are multi-pane gated and adjust the nearest focused split by 5%, grow the correct side, clamp at 10–90%, persist, remount only GTK containers, and retain/focus the same Ghostty surfaces. Nested/boundary tests prevent a clamped child from leaking resize to its parent. Real actions verified 0.50 → 0.45 → 0.50 → 0.10 plus an extra no-op with no diagnostics. GtkPaned release targets exact ordered pane identities, clamps once, and avoids allocation/motion write storms; a clean isolated launch left 0.500 unchanged. The release integration now overlaps 80 divider moves with 120 bounded Unicode lines, restores the split, requires an exact sentinel, and re-verifies focus/input/clipboard/close-risk on the same live surface; four consecutive X11/GLX passes succeeded. Trustworthy physical held-pointer evidence remains pending | None intended |
| Pane focus identity | `SessionStore`; terminal focus bridge | Catalog pane title/a11y copy | Exactly one active pane; key/menu/action/bottom-bar routes never lag visible focus. | `AwesoMuxApp.ApplicationState`; shim focus callbacks; shared focus reducer | partial | Sidebar routing plus focus-enter callbacks from both real surfaces pass in the release integration harness. Split immediately focuses the minted identity; multi-pane close chooses the next traversal neighbor and rebinds the focused footer before the old surface is retired. Real persisted action QA verified both transitions | None intended |
| Focus pane 1–6 | shortcut catalog | `Focus Pane 1` … `Focus Pane 6` | Depth-first direct focus; disabled out of range; higher panes remain reachable sequentially. | command catalog + shared indexed focus + GTK router | vertical slice complete | Exact one-based/no-op/bounds tests pass. Real action introspection on two panes exposed 1/2 enabled and 3–6 disabled; Pane 2 activation persisted its exact identity while disabled Pane 3 was inert | Modifier labels differ |
| Previous/next pane | shortcut catalog | `Previous Pane`; `Next Pane` | Cycle all panes in depth-first order. | shared traversal + GTK router | vertical slice complete | Existing wrap test plus real Previous/Next action activation across two live surfaces persisted the expected identities and refreshed focused chrome | None intended |
| Move/swap pane | shortcut catalog | `Move Pane Up/Down/Left/Right`; `Swap Pane With Next` | Deterministic layout mutation, disabled on no-op, focus follows moved identity. | shared layout reducer | not started | Geometry/property tests | None intended |
| Rename pane | shortcut catalog | `Rename Pane…` | Pin custom pane title only when multi-pane semantics allow. | shared models + dialog | not started | Rename/live-title precedence tests | None intended |
| Close pane | ADR-0002; `DestructivePaneActionConfirmationPolicy`; shortcut catalog | `Close Pane`; `Close pane in …?`; `Pane closed` | Multi-pane removes pane; last pane soft-closes workspace; risk confirmation and focus recovery. | shared close reducer + GTK remount + deferred Ghostty retirement | partial | Ctrl-W/app action uses per-pane live process/prompt/agent risk, exact bidi-safe title/body/hint, and the owned safe-default sheet. Confirmed multi-pane close removes only the focused identity, selects its next traversal neighbor, prunes stale acknowledgement/unanswered state, refreshes every projection, requests process close, and releases the detached surface only after exit; last-pane routing uses the existing soft-close workspace funnel. Live AT-SPI confirmed two pane closes across right/down remounts with the original pane preserved and no GTK/runtime diagnostics; both final images were inspected | Super replaces Command internally; visible reference hint remains exact |
| Restart shell | command palette | `Restart Shell` | Confirm risky restart and recreate only the target pane surface/session as specified. | terminal lifecycle coordinator | not started | Process/lifecycle test | None intended |
| Real Ghostty rendering | `GhosttyRuntime*`; `GhosttySurface*`; canonical Ghostty | Terminal content has no app copy | Full Ghostty runtime/renderer, not a replacement VT renderer. | `ghostty-shim/`; `AwesoMuxTerminal` | vertical slice complete | Real shell/output/render screenshot and standalone integration harness | GTK/OpenGL host replaces AppKit/Metal host |
| Multiple Ghostty surfaces | terminal bridge | N/A | Concurrent independent surfaces, output, focus, resize, close/recreate. | shim + `AwesoMuxTerminal` | vertical slice complete | Three 100-cycle two-busy-surface runs; pane-independence harness | None intended |
| Keyboard input | `GhosttyInputMapper.swift`; command policy ADR-0020 | Shortcut catalog owns app chords | Correct press/release/modifiers; terminal gets non-app keys; no parallel Ghostty app commands. | shim input bridge | partial | Text and Enter injection plus real X11 event path work; full key/modifier matrix pending | Linux keymap APIs |
| Unicode/IME input | `GhosttySurfaceTextInputClient.swift` | N/A | Composed Unicode, emoji, combining marks, wide characters, preedit/commit, UTF-8 locale fallback. | GTK IM context in shim | partial | Automated UTF-8/emoji/combining/wide-character round trip passes; desktop IME pending | GTK IME replaces NSTextInputClient |
| Mouse/selection/hover/scroll | `GhosttySurfaceInputBridge.swift` | N/A | Buttons, motion, selection, link hover, wheel/trackpad scroll and scale are pane-local. | shim pointer bridge | partial | Event controllers and translation implemented; interaction suite pending | Toolkit event APIs only |
| Clipboard | Ghostty action callbacks | Catalog confirmation/error copy | Standard/primary selection, paste/read/write, permission/confirmation paths, no logging. | shim + GTK clipboard coordinator | partial | Standard text read/write integration passes; primary/binary/denial/teardown cases pending | Linux primary selection is additive |
| Terminal search | shortcut catalog; Ghostty search callbacks | `Find in Pane` | Search active pane with result navigation/state and keyboard focus recovery. | shim search ABI + GTK overlay | not started | Search navigation test | None intended |
| Scrollback sheet | shortcut catalog | `Show Scrollback` | Bounded active-pane dump, correct empty/error state. | shim history + GTK sheet | not started | Large/empty scrollback test | None intended |
| Terminal title/cwd/progress/bell | Ghostty action bridge | Catalog title/progress/a11y copy | Pane-scoped callbacks update safe UI state; stale surfaces cannot publish. | shim callbacks + shared reducers | partial | Language-neutral title/PWD callbacks are exercised by real OSC 2/7 terminal integration; sanitized identity/generation-scoped mutations update focused row, search, accessibility, window, and footer state. Progress/bell actions and explicit teardown-race injection remain pending | None intended |
| Live-title one-clock rule | ADR-0031 | Dynamic titles/catalog announcements | Row, search, ordinals, palette, and accessibility share one coarse title tick. | authoritative pane snapshot + shared projection | partial | A single GTK-main publication updates the pane snapshot before all row/search/lifted/accessibility/window consumers reproject; user-renamed workspace titles remain authoritative. Timed coalescing and palette/jump-number consumers remain pending | None intended |
| Focused-pane bottom bar | `TerminalPathBar*` | Catalog path/Git/PR/action copy | Visible repo/path/actions always follow focused pane and fail closed on stale identity. | `AwesoMuxCore.FooterModels`; `AwesoMuxApp.FooterViews` | partial | Reference 38/24-point geometry, 10/11-point type hierarchy, 5-point pills, state-colored hairlines, icon/text grouping, exact `repo root` copy, and repo-relative nesting are implemented. Real X11 captures cover normal Git, synthetic long repo/path/branch/dirty states, and the isolated local-repo branch context; project/path/240-point branch truncation preserves all adjacent controls. A valid zero-group/no-selection capture verifies neutral absence: no focused footer or disabled path/Git/PR/CI/remote placeholders are fabricated. Sanitized branch/dirty/upstream/PR/CI/editor actions follow direct focus, and draft/review PR chip wording matches the reference. The tested gate now matches the reference: local shell-session ownership (no declared agent) permits staging while remote/agent panes fail closed; payloads omit the newline so active commands/TUIs remain user-controlled. Live AT-SPI changed the same branch row from copy-only under Codex to insertion under a local shell and invoked staging without execution. Branches fall back to copy; PR/CI insertion rows are omitted and re-check focused identity at activation; current-branch and exact action copy match the reference. Authenticated live PR/CI and modifier-click parity remain. | Files/GIO replaces Finder/Launch Services; Linux system-mono glyph outlines differ |
| Git repository context | `GitRepositoryContext*` | Catalog branch/change labels | Show repo root, branch, dirty/change state for focused local pane. | `AwesoMuxCore.TerminalFooterResolver` | partial | Nested temp-repo/path-presentation, porcelain, branch sanitization/quoting, bounded runner, and real dirty/ahead screenshot pass; disappearing status clears stale chips. Cache/freshness tuning pending. | None intended |
| Pull-request context | PR services/path bar | Catalog PR/open/error copy | Resolve PR for focused repository without leaking auth data or stale results. | `AwesoMuxCore.PullRequestStatus`; GTK PR chip/menu | partial | OPEN/closed/unsafe-URL parser tests; bounded prompt-free `gh` lookup and open/copy/insert actions implemented; authenticated live-PR fixture pending | GIO browser opener differs internally |
| App menus | `AwesoMuxApp.swift`; shortcut catalog | `File`, `Edit`, `View`, `Workspace`, `Pane`, `Window`, `Help` and catalog items | Complete dynamic enablement/titles and one command router. | GTK application actions/menu model | partial | File/View/Workspace/Pane models and native accelerators launch without parser warnings; dynamic enablement and remaining menus pending | GTK/global-menu presentation varies |
| Context menus | sidebar/pane/document views | Catalog baseline | Same available actions, destructive gates, and live enablement per target. | GTK popovers | partial | Workspace rows expose truthful rename, acknowledge, mute/unmute, pane jump, pin/unpin, bounded reorder, cross-group move, soft-close, and confirmed clear actions. Regular and lifted rows share secondary-click plus Menu/Shift+F10 access; menus rebuild against current identity after mutations. Group headers expose the implemented creation, rename, color, reorder, and confirmed close routes. Pane/document surfaces and full Orca invocation remain pending | Presentation only |
| Command palette | command palette models/views | `Command Palette`; exact action catalog | Search workspaces/actions/presets; one snapshot per summon; command routing parity. | shared command catalog + GTK overlay | not started | Search/routing/repeat tests + screenshot | Shortcut modifier mapping only |
| Shortcut catalog and overrides | `KeyboardShortcutCatalog.swift`; `docs/shortcuts.md` | 43 literal IDs plus generated workspace/pane bindings | One source drives menus, palette, settings, cheatsheet, validation, collisions. | `AwesoMuxCore.CommandCatalog` | partial | Initial exact-wording catalog has unique ID/chord tests and drives GTK actions. Focus Sidebar now matches the reference action and maps Command-Control-S to Linux Control-Super-S; its expanded, collapsed, and hidden handoffs were exercised through `org.gtk.Actions`. Full 43-ID parity and overrides pending | Control/Alt/Super mapping documented |
| Keyboard cheatsheet | shortcut catalog; cheatsheet view | `Keyboard Shortcuts`; `Show cheatsheet` | Searchable, grouped, live override display, keyboard dismiss/focus restore. | GTK overlay | not started | Keyboard-only + screenshot | Native window chrome differs |
| General settings | `GeneralSettingsPane.swift`; config | `General` plus catalog baseline | Every current general setting, default, validation, persistence, help text. | `AwesoMuxCore.AppPreferences`; GTK Quick Settings | partial | Profile-scoped owner-only theme, density, and notification preference round trip is implemented in Quick Settings. The prior placeholder `More settings…` window is intentionally no longer exposed; the real full settings catalog remains pending | Platform-specific launch behavior may differ |
| Appearance settings | `AppearanceSettingsPane.swift`; design tokens | `Appearance`; exact theme/accent/font labels | Theme, accent, glow, sidebar options, terminal background/theme/font previews. | `AwesoMuxCore.AppTheme`; GTK CSS/Quick Settings | partial | System/Light/Dark selector persists and reapplies app chrome; accent/font/preview and HC work pending | Font discovery API differs |
| Terminal settings | `TerminalSettingsPane.swift`; config | `Terminal`; exact cursor/CRT/bridge labels | Every terminal toggle and validation; next-lifecycle semantics where specified. | shared config + runtime | not started | Toggle/runtime lifecycle tests | None intended |
| Agents settings | `AgentsSettingsPane.swift`; ADRs 0010/0012/0017 | `Agents`; provider/card/status/action copy | Claude, Codex, OpenCode, Pi, Grok enable/install/status/uninstall/repair flows. | agent integration services + GTK settings | not started | Provider fixtures and modified-file tests | Provider CLIs/paths are Linux-specific inputs |
| Notification settings | `NotificationSettingsPane.swift` | `Notifications`; permission/mute/quiet copy | Global policy, permission state/remediation, muted workspace list, Dock-bounce equivalent decision. | app preferences + workspace snapshot overrides | partial | Global and per-workspace mute toggles persist owner-only/backward-compatibly; row menus switch exact Mute/Unmute wording. Notification adapter, permission/remediation, muted-list UI, and focus matrix remain pending | No Dock bounce; Linux attention mechanism needs decision |
| Workspace settings | `WorkspaceSettingsPane.swift` | `Workspaces`; exact restore/promotion copy | Restore, Needs Input promotion, workspace defaults, associated descriptions. | shared config + GTK settings | not started | Defaults/migration/UI tests | None intended |
| Keys settings | `KeysSettingsPane.swift`; catalog | `Keys`; shortcut validation strings | Rebind/reset/collision/reserved-key handling and cheatsheet entry point. | shared catalog + GTK settings | not started | Full binding round-trip tests | Linux reserved chords differ |
| Advanced settings | `AdvancedSettingsPane.swift` | `Advanced`; catalog baseline | All advanced and privacy-sensitive toggles with exact warnings. | shared config + GTK settings | not started | Config round-trip and warning tests | Platform-only options recorded individually |
| Diagnostics settings | `DiagnosticsSettingsPane.swift`; ADR-0008 | `Diagnostics`; privacy disclosure copy | Explicit refresh, bounded local process metrics, ephemeral history, no upload/content. | Linux `/proc` sampler + GTK settings | not started | Privacy-field and lifecycle tests | `/proc` replaces macOS process APIs |
| Settings persistence | `AwesoMuxConfig`; TOML | Validation/error catalog copy | Preserve unknown keys, atomically write owner-only `config.toml`, migrate safely. | shared config | not started | Golden TOML/permissions tests | XDG config path; session JSON persistence is implemented separately |
| Agent state vocabulary | `AgentState.swift`; ADR-0007 | `Idle`, `Running`, `Waiting`, `Thinking`, `Output`, `Needs Attention`, `Done`, `Error` | Pane-scoped semantic states and exact visual/notification priorities. | `AwesoMuxCore.AgentFooterSummary`; GTK sidebar footer/activity panel | partial | One priority-grouped pane roster drives counts, headings, stable traversal order, selected identity, titles, sanitized local/remote locations, expanded filtering, and exact-pane routing. Expanded controls announce panel open/close; collapsed 32-point state controls wrap only through matching panes. Real grouped-panel, collapsed-footer, and live blocking-input transition states were inspected. Live background Codex Done/Error crossings emitted exact medium-priority AT-SPI completion/error announcements while Running stayed silent and the workspace remained in its origin group. Quick Settings and Help & Feedback popovers were opened through AT-SPI and captured with truthful controls only; the mute row has owned theme contrast/focus and a 24-point target. The remaining live theme/accessibility matrix and audible Orca output remain pending | GTK symbolic/dot treatment replaces SF Symbols |
| Agent runtime side channel | `AgentRuntimeEvent*`; integration resources | Provider/error catalog copy | Capped owner-only structured events; exact source/provider identity; no content fields. | `AgentRuntimeEventProtocol`; `AgentEventEndpoint`; `AgentEventWatcher`; Ghostty environment ABI | vertical slice complete | Tests reject oversized, malformed, wrong-version, and unknown-provider events; endpoints are pane-scoped and `0600`; the watcher requires an owner-only regular file and never reads terminal content. The terminal integration proves exact environment delivery. A real Grok `userInputRequired` event updated the authoritative pane, Needs Input projection, provider tile, footer count, and persisted snapshot behind stable generation guards. Phase-aware waiting notifications now drive the non-persistent unanswered-turn projection and exact accessible transition wording without conflating permission prompts | Bounded background polling replaces the reference kqueue watcher; no user-visible difference |
| Claude Code integration | Claude resources/helper | Provider card and status catalog copy | CLI plugin install, lifecycle events, permission requests, transcripts/resume. | shared resources + Swift services | not started | Fixture + live provider smoke | Linux helper path/render |
| Codex integration | Codex resources/helper | Provider card, `Needs review`, `Needs repair`, `Unsupported` | CLI/app-server status, install without auto-trust, exact transcript/resume. | shared resources + Swift services | not started | RPC/version/trust drift tests | Linux executable discovery |
| OpenCode integration | OpenCode template; ADR-0010 | Provider card/status copy | Explicit enable + owned file drop; live gate; safe modified-file uninstall. | shared resources + Swift services | not started | Temp-home fixture tests | XDG provider path |
| Pi integration | Pi template; ADR-0010 | Provider card/status copy | Explicit enable + owned file drop; lifecycle and exact transcript/resume where available. | shared resources + Swift services | not started | Temp-home fixture tests | Provider home path |
| Grok integration | Grok plugin tree; ADR-0017 | Provider card/status copy | Native CLI plugin install/status, current + legacy hook compatibility, Grok identity glyph. | shared resources + Swift services | not started | CLI fixture/hook mapping tests | Linux executable/home discovery |
| Notifications/actions | notification policy/tracker/bridge | Catalog notification/action copy | Selected pane quiet; background attention; action returns to exact pane if still valid. | shared policy + Linux notification adapter | not started | State/focus/stale-action tests | freedesktop notification actions |
| Permission decision card | ADR-0028; permission bridge | Allow/deny/fallback catalog copy | Bridge primary, owner-only file fallback, timeout falls through without allow/deny. | amx protocol + GTK card | not started | Timeout/daemonless/forgery/focus tests | Unix socket API equivalent |
| `amx` persistent sessions | ADR-0011; zmx fork | Backend/error/reconnect catalog copy | Default local daemon per terminal session, detach/reattach, bounded respawn, no data loss. | `vendor/zmx`; backend service | not started | Kill/relaunch/reattach/scrollback tests | Linux process inspection differs |
| Session Manager | session manager sources | `Session Manager`; daemon state/action catalog copy | Show owned/restorable/abandoned/busy/idle/pinned sessions and safe actions. | GTK auxiliary window | not started | Live daemon fixture + screenshot | Native chrome differs |
| `AWESOMUX_AMX`/`$ZMX_SESSION` | `docs/amx-automation.md` | Environment variable names are exact | Correct binary/session addressing, stale-path guard, short socket namespaces. | launch environment service | not started | In-pane environment test | XDG runtime dir replaces macOS TMPDIR policy if validated |
| `amx send` | automation doc/zmx fork | `send` command is exact | Explicit session, caller supplies `\r`, silent-drop caveats; verify by history. | zmx fork + docs/tests | not started | Text/control/missing-session tests | None intended |
| `amx history`/cwd/wait/list | automation doc/zmx fork | Command names/flags exact | Preserve documented semantics and failure behavior; active foreground-job cwd. | zmx fork + services | not started | CLI contract tests | Linux `/proc` cwd implementation |
| SSH workspace creation | ADR-0023; remote models | `New Remote Workspace Group…`; `Connect via SSH…` | Declared `PaneExecutionPlan` builds OpenSSH args; never infer authority or fall back local. | shared execution model + process launcher | not started | Alias/auth/host-key/failure tests | Linux OpenSSH binary path |
| SSH credential custody | ADR-0022 | Security/error copy from catalog | Never store/prompt/cache/transmit passwords, passphrases, or keys; preserve host-key checks. | process launcher/security tests | not started | Argument/env/log audit | OS keychain wording becomes agent/config wording |
| Local-amx remote persistence | ADR-0023 | Reconnect/error catalog copy | Local daemon owns SSH child; relaunch reattaches; remote agent richness may degrade separately. | amx + SSH plan | not started | Disconnect/relaunch/host failure tests | None intended |
| Remote-owned zmx | ADR-0023 amendment | Backend discovery/disconnected copy | Optional declared remote owner; discover on far login shell; no local daemon/features; fail visibly. | SSH plan + lifecycle | not started | Backend absent/exit/status tests | macOS wrapper exit-file mechanism can be simplified on Linux only with evidence |
| Remote helper | `docs/remote-linux-helper.md`; bridge protocol | Install/remediation catalog copy | Optional compatible helper, explicit approved remediation, never ordinary SSH prerequisite. | shared helper protocol | not started | Version/missing/tampered tests | Linux app is local host too; destination behavior unchanged |
| File handoff | ADR-0023 INT-699 | Confirmation/error/path insertion copy | One bounded item, confirm host/name, unique remote path, revalidate pane, insert without submit. | handoff coordinator | not started | Host-switch/race/failure/input tests | Clipboard/file chooser APIs differ |
| Markdown document panes | CONTEXT; ADR-0018 | `Open Markdown File…`; document catalog copy | Local ≤2 MiB `.md`/`.markdown`, auxiliary to terminal, rendered links/tabs/annotations. | shared Markdown model + GTK text renderer | not started | Parser/size/link/UI tests + screenshot | GTK text layout replaces TextKit 2; equivalent behavior required |
| Remote Markdown snapshots | declared identity model | Read-only/offline/error catalog copy | Typed `ResourceIdentity`, declared SSH authority only, bounded secure cache, fail closed. | shared resource model + SSH fetch | not started | Alias/offline/relative/stale identity tests | XDG cache path |
| Markdown annotations | CONTEXT; Markdown sources | `Send to Agent`; marker grammar/help copy | Whole-doc and inline AMX markers, resolved lifecycle, live reload, safe source ranges. | shared Markdown engine + GTK renderer | not started | Round-trip/selection/table tests | Rendering implementation differs |
| Agent transcripts | ADR-0033 | `Open Agent Transcript`; `Resume Agent Session` | Exact provider-native identity only; bounded secure render; stage resume without submitting. | shared transcript adapters | not started | Wrong-session/traversal/stale-focus tests | Provider log roots differ by environment |
| Layout presets | ADR-0027 | `Save Layout as Preset…`; `Apply Layout Preset…`; `Apply Layout: <name>` | Strict untrusted JSON under `.awesomux/layouts`; apply creates new local workspace. | shared preset model/store | not started | Traversal/symlink/depth/version tests | None intended |
| Floating Panel | ADR-0024/0030/0032 | `Show Floating Panel`; `Hide Floating Panel` | Workspace-scoped scratch terminal, Escape smart-dismiss, Cmd-W hide, risk confirmation. | GTK auxiliary panel | not started | Keyboard/process/workspace tests + screenshot | Window-manager positioning constraints |
| Terminal Companion | ADR-0024/0030/0032 | `Show Terminal Companion`; `Minimize Terminal Companion` | App-wide persistent terminal; Escape goes to terminal; Cmd-W/minimize keeps process; close ends it. | GTK auxiliary panel | not started | Cross-workspace/minimize/input tests + screenshot | Native panel chrome differs |
| About/window-like auxiliary surfaces | ADR-0032 | About/session/worktree/cheatsheet catalog titles | Window-like surfaces use platform window controls; controllers own teardown. | GTK windows | not started | Close/focus/lifecycle tests | Linux decorations/window manager |
| Bundled UI font | DesignSystem font resources | `Geist` | Register/package four Geist weights and use matching typography scale. | app resources; `BundledFonts`; GTK CSS | complete | The four pinned OFL Geist faces are packaged as SwiftPM resources, registered process-locally through Fontconfig, and applied to proportional chrome; inspected Latte/Mocha real-window renders use the bundled family | Fontconfig process-local registration replaces CoreText |
| Bundled terminal font | Hack Nerd Font resources | `Hack Nerd Font Mono`; `System default` | Package four faces/license; default terminal stack; user override/reset semantics. | shared resources + Ghostty config | not started | Font selection/render test | Fontconfig replaces CoreText |
| Colors/tokens/themes | `Sources/DesignSystem/Tokens`; ADR-0013/0029 | `Peach`, `Mauve`, `Sapphire`, `Green`; theme labels | Catppuccin Mocha/Latte/HC, accents, dividers, focus ≥3:1, text contrast, custom/Ghostty modes. | `ChromeStyles`; `ChromeAppearancePolicy`; GTK Quick Settings | partial | Owned focused stylesheet now implements Mocha and Latte chrome, reference HC foreground/focus treatment, system appearance, explicit Light/Dark, reduced-motion detection, density, and state tints. Policy tests cover GTK theme/override resolution; real Latte/Mocha windows were inspected. HighContrast Quick Settings and full group action/color popovers were opened through AT-SPI and inspected with stronger borders plus distinct labels/selections/checks. Accent selection, custom/Ghostty modes, automated contrast and provider/status grayscale matrices remain pending | GTK CSS and Fontconfig replace SwiftUI environment/CoreText |
| Provider glyphs/status shapes | ADR-0016/0017; `AgentTile.swift` | Provider names/state labels | Distinct owned glyphs, not color-only; same provider tint family. | `SidebarAgentTilePresentation`; `AgentGlyphDrawing`; GTK overlay tiles | complete | The tested projection maps Claude, Codex, OpenCode, Pi, Grok, and shell, rolls workspace state up by pane priority, adds non-color-only state badges, and publishes provider/status accessibility wording. Owned scalable Cairo geometry independently implements the pinned eight-ray burst, open spiral, bracket pair, three interlocking rings, and shell prompt; Pi uses the owned mono stack. Decorative glyph children are AT-SPI hidden. Expanded 1440×852 real-app matrices were inspected in Latte, Mocha, and HighContrast; the same component supplies lifted, pane-peek, and rail sizes | Cairo drawing replaces SwiftUI Shape; appearance and semantics are intended to match |
| App icon | `Resources/AppIcon.icon` | `awesoMux` | Use existing project-owned artist-created icon without generative alteration. | shared icons + desktop package | not started | Installed desktop/app switcher inspection | Linux icon sizes/format |
| Localization/plurals | `Localizable.xcstrings`; stringsdict; ADR-0014 | Entire catalog baseline | Literal-as-key boundary, real plural forms, no manual singular/plural drift. | shared text baseline + localization adapter | partial | Baseline generation/drift check exists; runtime lookup pending | gettext/Swift localization choice pending |
| Third-party licenses | `THIRD_PARTY_NOTICES.md`; `Resources/Licenses` | License/component names | Complete notices for shipped dependencies/resources; no GPL runtime/source contamination. | `THIRD_PARTY_NOTICES.md`; `shared/resources/licenses` | not started | Source/license audit | Linux packaging notice locations |
| Desktop integration | macOS app bundle/release docs | `awesoMux` | App launcher, icon, MIME/file opening, terminal category, clean uninstall/state rules. | `packaging/linux` | not started | Install from clean package and launch | Freedesktop desktop entry/package formats |
| Packaging | ADR-0019 as behavior reference | Version/update/error copy | Reproducible clean local package with embedded runtime/resources and verification. | `script/package.sh`; packaging | not started | Clean-checkout package test | Linux signing/distribution decision pending |
| Wayland | Linux track prompt | N/A | Full terminal/input/clipboard/IME/scale/accessibility operation. | GTK/shim | blocked | Real native run fails GTK desktop-GL context creation on current COSMIC/NVIDIA stack | Temporary XWayland constraint recorded |
| X11 | Linux track prompt | N/A | Same supported product behavior under GTK X11 backend. | GTK/shim | vertical slice complete | Full integration harness, three lifecycle stress runs, and screenshot under GLX | Linux-only target |
| Scaling | root verification contract | N/A | Correct at 100%, 125%, 150%, 200%; no blurry/misaligned terminal or clipped controls. | GTK CSS/layout/shim | partial | A real `GDK_SCALE=2` X11/GLX capture verifies the 296-point sidebar becomes 592 physical pixels, long labels truncate, and live-allocation split restoration keeps both Ghostty panes visible. Fractional 125%/150%, blur, and pointer-alignment inspection remain pending | Desktop fractional-scaling support varies |
| Accessible text scaling | root verification contract | N/A | Chrome text follows system accessibility scaling without clipped controls, lost minimum targets, or moving the sidebar boundary. | `ChromeTextScalePolicy`; `ChromeStyles` | complete | GTK Xft DPI and the standard X11 DPI override drive a tested, clamped 1–2× rewrite of owned CSS font sizes. A real 1.5× X11/GLX capture verifies titlebar, search, lifted rows, groups, creation controls, sidebar footer, focused footer, and long-text truncation at fixed 296 px width with two live Ghostty panes | GTK exposes DPI rather than SwiftUI Dynamic Type categories |
| RTL layout | root verification contract | N/A | Direction-sensitive sidebar, headers, rows, controls, footers, and panes mirror without clipping or broken routing. | GTK locale direction + owned chrome | complete | A real `ar_EG.utf8` X11/GLX capture verifies mirrored sidebar/titlebar placement, split-create segment order, row/group/options alignment, both footer surfaces, and stable two-pane Ghostty order at 1440×852 | Runtime translation remains in the localization phase; this row covers layout direction |
| Security/privacy | ADR-0008; Linux prompt | Privacy/warning catalog copy | No terminal/content logging, owner-only stores, validated FFI/input, argv process spawn, no silent SSH weakening. | all layers + preflight audits | not started | Security review and negative tests | Platform syscalls differ |
| Local verification | Linux prompt | Local command output only | `./script/preflight.sh` owns format/static/tests/license/submodule/patch/release checks. No CI. | `script/preflight.sh` | partial | Static checks, model tests, release build, terminal integration, and lifecycle stress wired locally | Intentional Linux repository policy |
